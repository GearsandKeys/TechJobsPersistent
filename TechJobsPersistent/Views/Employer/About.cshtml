@model List<TechJobsPersistent.Models.Employer>

@if (Model.Count == 0)
{
    <p>No information provided.</p>
}
else
@foreach (Employer boss in Model)
{
    <h1>Employer: @boss.Name </h1>
    <div>
        <p>Name: @boss.Name </p>
        <p>Location: @boss.Location</p>
    </div>

}
